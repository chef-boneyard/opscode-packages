# Automatically added by dh_installinit
if [ -x "/etc/init.d/chef-solr" ]; then
	update-rc.d chef-solr defaults >/dev/null
	invoke-rc.d chef-solr start || exit $?
fi
# End automatically added section
