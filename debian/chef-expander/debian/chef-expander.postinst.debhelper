# Automatically added by dh_installinit
if [ -x "/etc/init.d/chef-expander" ]; then
	update-rc.d chef-expander defaults >/dev/null
	invoke-rc.d chef-expander start || exit $?
fi
# End automatically added section
