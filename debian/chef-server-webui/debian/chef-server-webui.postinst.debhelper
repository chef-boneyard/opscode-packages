# Automatically added by dh_installinit
if [ -x "/etc/init.d/chef-server-webui" ]; then
	update-rc.d chef-server-webui defaults >/dev/null
	invoke-rc.d chef-server-webui start || exit $?
fi
# End automatically added section
